# Install

Copy this folder into the root of the `Denis-Krueger-labs/Denis-Krueger-labs` profile repository.

Important:

- Preserve your existing `assets/activity.svg` and its generator.
- The moth is embedded directly into `assets/header.svg`.
- Run `python scripts/build.py` before pushing.
- The workflow validates generated SVG assets after pushes and once per day.
